<h1>Vote Result</h1>

<p>
  <h3><%= Movie.find(Vote.find(params[:id]).upvote_id).title %></h3>
  <br />
  <h4>TROUNCES</h4>
  <%=  %>
  <br />
  <h3><%= Movie.find(Vote.find(params[:id]).downvote_id).title %></h3>
</p>
