<h2>VOTE RECORDS</h2>

<p>
<%= link_to("Back", "/votes/show/#{Vote.order(:id).last.id}") %>
<%= link_to("Vote Again?", "/", {class: "new_vote"}) %>
<%= link_to("Log Out", destroy_user_session_path, :method => :delete) %>
</p>

<p>
  <h3>MOVIE RANKS</h3>
  <% rank = 0 %>
  
  <% Vote.movie_rank.each do |movie| %>
    <%= link_to("#{Movie.find_by_id(movie[0]).title}", "/movies/index/#{movie[0]}") %>
    <br />
    Rank: <%= rank += 1 %>
    Win Percent: <%= movie[1] %>
    <br />
  <% end %>

  <br />

  TOTAL VOTES: <%= Vote.ids.count %> and counting...
</p>